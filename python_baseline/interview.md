# Reflection Prompts

Answer these in your own words after you attempt the coding exercise.

1. What is a variable?
Ans: A variable is like an entity that stores a value like a number, string, object 
2. What is the difference between a list and a dictionary?
 A list can have repititve values and dictionary can have only unique values. i think list is like an array, i dont really know the exact difference.
3. What does a function parameter do?
  a function parameter define the values coming into the function. I dont know if like C++ there is pass by value and reference. i am not sure if that exists in python. but normally the function parameters define the values that the function is passed on.
4. What does `return` do?
 return is like the end result after the function that we return as the result. like if we call a function add(), we return the value of the add.
5. What does `None` mean in Python?
 not sure....but maybe None is Null
6. What is a loop for?
Loop is for repeating a certain set of statements multiple times, there is for, while loops
7. When would you use an `if` statement?
 Ans: it is a conditional statement used to check a statement.
8. What does a traceback help you figure out?
 i dont know whats exactly a traceback. i am just guessing, is it something like when we click on a function name in a IDE, it takes to the place where it is used. so we use it for debug.
9. What is a unit test checking?
i dont know really the difference between unittest or pytest, i just know that they are used to test  some small part of code such as a function i guess. I dont really understand the difference betweeen writing a test and writing a normal python script apart from just being the purpose to test.
10. Which coding task felt easiest, and which felt hardest?
I couldnt complete any coding task.
