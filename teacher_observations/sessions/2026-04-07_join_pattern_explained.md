# 2026-04-07 Join Pattern Explained

- The student explained the `join()` formatting pattern in plain English after completing `build_active_user_report(...)`.
- The explanation correctly covered the trailing-comma problem, why `join()` fixes it, and why the empty case still needs its own branch.
- Teaching implication: move to a fresh transfer task where the report values are numeric IDs rather than usernames.
